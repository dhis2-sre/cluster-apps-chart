apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-apps.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
